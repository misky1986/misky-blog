workflows:
  version: 2
  main:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master


version: 2.1
jobs:
  build:
    docker:
      - image: cibuilds/hugo:0.54    
    steps:
      - checkout      
      - run:
          name: "Run Hugo"          
          command: HUGO_ENV=production hugo -v -s /root/project/
      # - run:
      #     name: "Test Website"
      #     command: htmlproofer /root/project/public --allow-hash-href --check-html --empty-alt-ignore --disable-external
  deploy:
    docker:
      - image: cibuilds/hugo:0.54
    environment:
      - SOURCE_BRANCH: master
      - TARGET_BRANCH: gh-pages
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "32:7f:9d:65:03:9d:9b:26:3b:95:61:c0:be:0a:b8:ee"
      - run:
          name: Deployment started
          command: bash ./scripts/deploy-gh-pages.sh build